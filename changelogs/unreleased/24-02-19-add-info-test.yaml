changed:
  - Tracked the current test that is being run in the test runner, so that
    upon a test failure, we know where the failure is from
    (the existing test grouping should have made this unnecessary,
    but the grouping is not perfect.)
